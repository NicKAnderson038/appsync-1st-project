{
  "name": "appsync-1st-project",
  "version": "1.0.0",
  "main": "handler.js",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sandbox": "sls deploy --stage dev --region us-east-1 --verbose --aws-profile default"
  },
  "dependencies": {
    "serverless-appsync-plugin": "^1.0.5",
    "serverless-pseudo-parameters": "^2.2.0",
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "serverless-plugin-include-dependencies": "^3.1.1"
  }
}
